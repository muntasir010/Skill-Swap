import Hero from "../components/Hero";
import Items from "../components/Items";

const Home = () => {
    return (
        <div>
            <Hero />
            <Items/>
        </div>
    );
};

export default Home;